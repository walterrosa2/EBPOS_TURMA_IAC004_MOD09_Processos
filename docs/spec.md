# Especificação Técnica

Este documento detalhará os modelos de dados e a especificação da API e componentes.

Consulte o `prd.md` para os schemas iniciais do banco de dados (SQLite) e estrutura de respostas REST.
