Objetivo do Pacote 07 Ao final deste pacote, o projeto deve estar: ```text revisado tecnicamente com testes mínimos executáveis com documentação final atualizada com variáveis de ambiente organizadas com backend preparado para Railway com frontend preparado para Railway com SQLite usando volume persistente com deploy backend validado com deploy frontend validado com CORS configurado com teste de persistência pós-redeploy concluído com checklist de homologação do MVP documentado ``` --- # Regras obrigatórias 1. Não salvar secrets no GitHub. 2. Não commitar `.env`. 3. Não expor `OPENAI_API_KEY` no frontend. 4. Não registrar dados sensíveis em logs. 5. Não deixar `localhost` hardcoded em produção. 6. Não usar SQLite fora do volume persistente em produção. 7. Não fazer deploy antes de todos os testes locais passarem. 8. Não alterar endpoints sem necessidade. 9. Não quebrar compatibilidade com frontend. 10. Não implementar funcionalidades fora do escopo. 11. Atualizar documentação final. 12. Criar checklist de homologação. 13. Validar persistência após redeploy. 14. Validar erro amigável quando OpenAI não estiver configurada. 15. Validar erro amigável quando processo não tiver etapas. 16. Validar fluxo completo ponta a ponta. --- # TASK-100 — Revisão técnica geral ## Objetivo Revisar aderência do código ao PRD, arquitetura e pacotes anteriores. ## Arquivos impactados ```text docs/review.md docs/backlog.md docs/changelog.md ``` Criar: ```text docs/review.md ``` ## Checklist obrigatório Validar: ```text Backend: - main.py registra todas as rotas. - /health funciona. - CRUD de processos funciona. - CRUD de etapas funciona. - API de fluxo funciona. - API de análise IA funciona. - API de diretrizes funciona. - services concentram regra de negócio. - repositories concentram acesso ao banco. - schemas Pydantic validam entradas. - erros 400, 404, 422 e 500 são tratados. Frontend: - rotas principais funcionam. - layout SaaS permanece consistente. - catálogo de processos funciona. - editor visual funciona. - análise IA funciona. - automações funcionam. - chamadas HTTP estão centralizadas em services. - VITE_API_URL é usado corretamente. IA: - system prompt existe. - user prompt template existe. - resposta IA é validada antes de salvar. - JSON inválido não é persistido. - ausência de OPENAI_API_KEY gera erro controlado. Segurança: - .env está ignorado. - chaves não estão no código. - logs não expõem dados sensíveis. - frontend não possui chave OpenAI. Deploy: - backend possui comando de start. - frontend possui build funcional. - CORS está configurável. - SQLite usa caminho de volume em produção. ``` ## Critérios de aceite ```text Dado que a revisão foi concluída, quando abrir docs/review.md, então deve existir checklist técnico com status de cada item. Dado que algum item falha, quando registrar a revisão, então deve haver ação corretiva indicada. ``` --- # TASK-101 — Hardening de segurança e configuração ## Objetivo Garantir que variáveis, CORS, logs e secrets estejam corretos. ## Arquivos impactados ```text backend/app/core/config.py backend/app/main.py backend/.env.example frontend/.env.example .gitignore docs/deployment.md docs/security.md ``` Criar: ```text docs/security.md ``` ## Variáveis backend obrigatórias ```env APP_NAME=QDT Processos Contabeis API APP_ENV=production DATABASE_URL=sqlite:////app/data/qdt_processos.db CORS_ORIGINS=https://URL-DO-FRONTEND-RAILWAY OPENAI_API_KEY= OPENAI_MODEL=gpt-4o OPENAI_TIMEOUT_SECONDS=60 ``` ## Variáveis frontend obrigatórias ```env VITE_API_URL=https://URL-DO-BACKEND-RAILWAY ``` ## Regras - `.env` deve estar no `.gitignore`. - `.env.example` não pode conter segredo real. - `OPENAI_API_KEY` só existe no backend. - `CORS_ORIGINS` deve aceitar o domínio público do frontend. - Em desenvolvimento, `CORS_ORIGINS=http://localhost:5173`. - Em produção, não deixar wildcard `*`, salvo decisão temporária documentada. - Logs não devem imprimir payload completo da IA nem dados dos processos. ## Critérios de aceite ```text Dado que .env.example existe, quando abrir o arquivo, então não deve haver chaves reais. Dado que o frontend está em produção, quando chamar backend, então CORS deve permitir apenas o domínio configurado. Dado que ocorrer erro de IA, quando registrar logs, então o log não deve conter dados sensíveis do processo. ``` --- # TASK-102 — Testes automatizados finais do backend ## Objetivo Garantir que o backend tenha cobertura mínima do fluxo principal. ## Arquivos impactados ```text backend/tests/ docs/tests.md ``` ## Testes obrigatórios Garantir que existam e passem: ```text test_health.py test_processos.py test_etapas.py test_fluxo.py test_analise_schema.py test_ia_service.py test_analises_api.py test_diretrizes_api.py ``` ## Cenários mínimos ```text Health: - /health retorna 200. Processos: - cria processo válido. - bloqueia processo sem nome. - bloqueia processo sem área. - lista processos. - atualiza processo. - exclui processo. Etapas: - cria etapa válida. - bloqueia etapa sem nome. - bloqueia etapa para processo inexistente. - exclui etapa e remove conexões. Fluxo: - salva posição X/Y. - salva conexão. - bloqueia conexão com etapa de outro processo. - recupera fluxo persistido. IA: - schema aceita JSON válido. - schema rejeita prioridade inválida. - schema rejeita campo obrigatório ausente. - processo sem etapas retorna 400. - processo inexistente retorna 404. - análise mockada é salva. - JSON inválido da IA não é salvo. Diretrizes: - lista diretrizes. - atualiza status. - rejeita status inválido. ``` ## Comando obrigatório ```bash cd backend pytest ``` ## Critérios de aceite ```text Dado que pytest é executado, quando os testes terminarem, então todos devem passar. Dado que os testes de IA rodam, quando executados, então nenhuma chamada real à OpenAI deve ocorrer. ``` --- # TASK-103 — Checklist manual ponta a ponta ## Objetivo Validar o fluxo real do usuário antes do deploy. ## Arquivo impactado ```text docs/homologation.md ``` Criar: ```text docs/homologation.md ``` ## Fluxo de homologação obrigatório Executar manualmente: ```text 1. Abrir frontend local. 2. Confirmar backend local online. 3. Criar processo. 4. Editar processo. 5. Abrir detalhe. 6. Abrir fluxo visual. 7. Criar etapa 1. 8. Criar etapa 2. 9. Conectar etapa 1 à etapa 2. 10. Mover etapas. 11. Salvar fluxo. 12. Recarregar página. 13. Confirmar posições persistidas. 14. Confirmar conexões persistidas. 15. Voltar ao detalhe. 16. Gerar análise IA. 17. Abrir detalhe da análise. 18. Validar resumo executivo. 19. Validar maturidade. 20. Validar gargalos. 21. Validar riscos. 22. Validar sugestões de melhoria. 23. Validar sugestões de automação. 24. Abrir automações. 25. Alterar status de diretriz. 26. Recarregar automações. 27. Confirmar status persistido. 28. Testar processo sem etapas. 29. Confirmar erro amigável. 30. Simular backend fora do ar. 31. Confirmar erro amigável no frontend. ``` ## Critérios de aceite ```text Dado que o checklist foi executado, quando abrir docs/homologation.md, então cada item deve estar marcado como aprovado, reprovado ou pendente. Dado que algum item falha, quando documentar homologação, então deve existir descrição do problema e ação corretiva. ``` --- # TASK-104 — Preparar backend para Railway ## Objetivo Garantir que o backend suba corretamente na Railway. ## Arquivos impactados ```text backend/requirements.txt backend/railway.toml backend/README.md docs/deployment.md ``` Criar se não existir: ```text backend/railway.toml ``` ## Comando de start recomendado Usar Uvicorn com host e porta compatíveis com Railway: ```toml [deploy] startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT" ``` ## Variáveis Railway backend Configurar no painel Railway: ```env APP_ENV=production DATABASE_URL=sqlite:////app/data/qdt_processos.db CORS_ORIGINS=https://URL-DO-FRONTEND-RAILWAY OPENAI_API_KEY=INSERIR_NO_PAINEL_RAILWAY OPENAI_MODEL=gpt-4o OPENAI_TIMEOUT_SECONDS=60 ``` ## Volume SQLite Configurar volume no serviço backend: ```text Mount path: /app/data Database URL: sqlite:////app/data/qdt_processos.db ``` ## Regras - O backend precisa criar o diretório do banco se não existir. - O backend não deve usar `./data/qdt_processos.db` em produção. - O volume deve estar vinculado ao serviço backend. - Não configurar volume no frontend. - Validar logs após deploy. ## Critérios de aceite ```text Dado que o backend foi publicado, quando acessar /health, então deve retornar status ok. Dado que DATABASE_URL aponta para /app/data, quando criar processo, então o SQLite deve salvar dentro do volume. Dado que o serviço faz redeploy, quando voltar, então os dados devem continuar disponíveis. ``` --- # TASK-105 — Preparar frontend para Railway ## Objetivo Garantir que o frontend React/Vite faça build e seja publicado corretamente. ## Arquivos impactados ```text frontend/package.json frontend/railway.toml frontend/README.md docs/deployment.md ``` Criar se não existir: ```text frontend/railway.toml ``` ## Build local obrigatório Antes do deploy, executar: ```bash cd frontend npm install npm run build ``` ## Variável Railway frontend Configurar no painel Railway: ```env VITE_API_URL=https://URL-DO-BACKEND-RAILWAY ``` ## Observação sobre SPA Se o deploy do React não preservar rotas client-side após refresh, configurar fallback para SPA conforme estratégia escolhida no projeto. Opções aceitáveis: ```text 1. Usar template React/Caddy da Railway. 2. Usar Dockerfile com Caddy. 3. Usar configuração de fallback compatível com Railway. ``` ## Critérios de aceite ```text Dado que o frontend foi publicado, quando acessar a URL pública, então Dashboard deve carregar. Dado que o usuário acessa diretamente /processos, quando atualizar a página, então a rota não deve retornar 404 do servidor. Dado que VITE_API_URL está configurado, quando frontend chamar backend, então as requisições devem chegar na API de produção. ``` --- # TASK-106 — Configurar serviços Railway via GitHub ## Objetivo Criar deploy via GitHub com dois serviços separados. ## Estrutura esperada na Railway ```text Railway Project: qdt-processos-ctb Services: 1. qdt-backend Root directory: backend Start command: uvicorn app.main:app --host 0.0.0.0 --port $PORT Volume: /app/data 2. qdt-frontend Root directory: frontend Build command: npm run build Start/static strategy: conforme template ou configuração escolhida ``` ## Passos ```text 1. Criar projeto na Railway. 2. Conectar repositório GitHub. 3. Criar serviço backend apontando para /backend. 4. Criar serviço frontend apontando para /frontend. 5. Configurar variáveis backend. 6. Configurar volume backend em /app/data. 7. Fazer deploy backend. 8. Gerar domínio público do backend. 9. Configurar VITE_API_URL no frontend com URL do backend. 10. Fazer deploy frontend. 11. Gerar domínio público do frontend. 12. Atualizar CORS_ORIGINS do backend com URL do frontend. 13. Redeploy backend. 14. Testar fluxo completo. ``` ## Critérios de aceite ```text Dado que os serviços foram criados, quando abrir Railway, então deve haver um serviço backend e um serviço frontend. Dado que o backend tem domínio público, quando acessar /health, então deve retornar ok. Dado que o frontend tem domínio público, quando abrir a aplicação, então deve carregar a interface. Dado que CORS está configurado, quando frontend chamar backend, então a requisição deve ser permitida. ``` --- # TASK-107 — Teste de persistência SQLite em produção ## Objetivo Confirmar que o SQLite está persistindo no volume Railway. ## Procedimento obrigatório ```text 1. Abrir frontend em produção. 2. Criar processo chamado "Teste Persistência Railway". 3. Criar pelo menos uma etapa. 4. Salvar fluxo. 5. Confirmar que processo aparece no catálogo. 6. Fazer redeploy do backend. 7. Aguardar backend voltar. 8. Abrir frontend novamente. 9. Confirmar que o processo ainda existe. 10. Confirmar que etapa e fluxo ainda existem. 11. Registrar resultado em docs/deployment.md. ``` ## Critérios de aceite ```text Dado que o backend foi redeployado, quando abrir o catálogo, então o processo criado antes do redeploy deve continuar existindo. Dado que o fluxo foi salvo antes do redeploy, quando abrir o editor visual, então as etapas e conexões devem continuar disponíveis. ``` --- # TASK-108 — Validação de produção ponta a ponta ## Objetivo Executar o fluxo principal na aplicação publicada. ## Checklist produção ```text 1. Abrir frontend Railway. 2. Ver Dashboard. 3. Criar processo. 4. Criar etapas. 5. Conectar etapas. 6. Salvar fluxo. 7. Gerar análise IA. 8. Abrir análise. 9. Abrir automações. 10. Atualizar status de diretriz. 11. Recarregar frontend. 12. Confirmar dados persistidos. 13. Abrir logs backend. 14. Confirmar ausência de erro crítico. 15. Confirmar ausência de dados sensíveis em logs. ``` ## Critérios de aceite ```text Dado que a aplicação está publicada, quando executar o fluxo principal, então todas as etapas devem funcionar sem erro crítico. Dado que há erro de OpenAI ou rede, quando o erro ocorrer, então o usuário deve receber mensagem amigável. ``` --- # TASK-109 — README final ## Objetivo Atualizar documentação final para uso, desenvolvimento e deploy. ## Arquivos impactados ```text README.md backend/README.md frontend/README.md docs/deployment.md docs/security.md docs/homologation.md ``` ## README raiz deve conter ```text Nome do projeto Objetivo Stack Arquitetura Estrutura de pastas Fluxo funcional Como rodar localmente Como rodar testes Variáveis de ambiente Como fazer deploy na Railway Limitações do MVP Roadmap ``` ## backend/README.md deve conter ```text Como criar ambiente virtual Como instalar dependências Como configurar .env Como rodar backend Como rodar pytest Endpoints principais Cuidados com OPENAI_API_KEY Configuração SQLite local e produção ``` ## frontend/README.md deve conter ```text Como instalar dependências Como configurar VITE_API_URL Como rodar frontend Como gerar build Rotas principais Cuidados com ambiente de produção ``` ## Critérios de aceite ```text Dado que um novo desenvolvedor abre o projeto, quando ler README.md, então deve conseguir entender stack, execução local e deploy. Dado que o operador precisa configurar Railway, quando ler docs/deployment.md, então deve encontrar variáveis, serviços e teste de persistência. ``` --- # TASK-110 — Fechamento do MVP e backlog futuro ## Objetivo Encerrar formalmente a versão MVP e registrar evolução. ## Arquivos impactados ```text docs/backlog.md docs/changelog.md docs/roadmap.md docs/mvp-acceptance.md ``` Criar: ```text docs/roadmap.md docs/mvp-acceptance.md ``` ## docs/mvp-acceptance.md Registrar checklist final: ```text [ ] Criar processo. [ ] Editar processo. [ ] Excluir processo. [ ] Criar etapas. [ ] Conectar etapas. [ ] Salvar fluxo. [ ] Reabrir fluxo salvo. [ ] Gerar análise IA. [ ] Exibir análise IA. [ ] Gerar diretrizes. [ ] Atualizar status de diretriz. [ ] Deploy backend. [ ] Deploy frontend. [ ] Persistência SQLite pós-redeploy. [ ] README final. [ ] Nenhum segredo versionado. ``` ## docs/roadmap.md Registrar próximos incrementos: ```text Fase 2 — Autenticação e usuários Fase 3 — Multiempresa Fase 4 — Upload e leitura de documentos Fase 5 — Exportação PDF/Markdown Fase 6 — Dashboard executivo avançado Fase 7 — Base de conhecimento/RAG Fase 8 — Integrações com sistemas contábeis Fase 9 — Execução de automações Fase 10 — Auditoria, logs e permissões ``` ## docs/changelog.md Adicionar: ```markdown ## 0.7.0 - Realizada revisão técnica geral. - Adicionado checklist de segurança. - Adicionado checklist de homologação. - Preparado backend para Railway. - Preparado frontend para Railway. - Configurado uso de SQLite com volume persistente. - Documentado processo de deploy. - Documentado teste de persistência pós-redeploy. - Atualizado README final. - Criado roadmap pós-MVP. ``` ## Critérios de aceite ```text Dado que o MVP foi homologado, quando abrir docs/mvp-acceptance.md, então deve existir checklist final preenchido. Dado que o MVP tem limitações, quando abrir docs/roadmap.md, então devem estar registradas próximas fases. ``` --- # Comandos locais finais ## Backend ```bash cd backend pip install -r requirements.txt pytest uvicorn app.main:app --reload ``` ## Frontend ```bash cd frontend npm install npm run build npm run dev ``` ## Health local ```bash curl http://localhost:8000/health ``` --- # Variáveis finais ## Backend local ```env APP_ENV=development DATABASE_URL=sqlite:///./data/qdt_processos.db CORS_ORIGINS=http://localhost:5173 OPENAI_API_KEY= OPENAI_MODEL=gpt-4o OPENAI_TIMEOUT_SECONDS=60 ``` ## Backend produção Railway ```env APP_ENV=production DATABASE_URL=sqlite:////app/data/qdt_processos.db CORS_ORIGINS=https://URL-DO-FRONTEND-RAILWAY OPENAI_API_KEY= OPENAI_MODEL=gpt-4o OPENAI_TIMEOUT_SECONDS=60 ``` ## Frontend local ```env VITE_API_URL=http://localhost:8000 ``` ## Frontend produção Railway ```env VITE_API_URL=https://URL-DO-BACKEND-RAILWAY ``` --- # Definition of Done do Pacote 07 A entrega só estará concluída quando: ```text [ ] Revisão técnica geral concluída. [ ] docs/review.md criado. [ ] docs/security.md criado. [ ] docs/homologation.md criado. [ ] docs/mvp-acceptance.md criado. [ ] docs/roadmap.md criado. [ ] pytest passa localmente. [ ] npm run build passa localmente. [ ] Backend preparado para Railway. [ ] Frontend preparado para Railway. [ ] Variáveis de ambiente documentadas. [ ] OPENAI_API_KEY não está no GitHub. [ ] VITE_API_URL não está hardcoded. [ ] CORS_ORIGINS configurado. [ ] Backend Railway responde /health. [ ] Frontend Railway carrega dashboard. [ ] Frontend Railway chama backend Railway. [ ] SQLite usa volume persistente. [ ] Dados persistem após redeploy. [ ] Fluxo completo em produção funciona. [ ] Logs não expõem dados sensíveis. [ ] README raiz atualizado. [ ] backend/README.md atualizado. [ ] frontend/README.md atualizado. [ ] changelog atualizado para 0.7.0. ``` --- # Restrições Não implemente neste pacote: ```text login multiusuário upload de documentos exportação PDF RAG integrações externas RPA real dashboard avançado mudança de modelo LLM mudança de banco de dados ``` --- # Resultado esperado Ao final deste pacote, o MVP estará pronto para homologação operacional e uso controlado. Fluxo final esperado: ```text Criar processo → mapear etapas → conectar fluxo → salvar fluxo → gerar análise IA → visualizar diagnóstico → acompanhar diretrizes → atualizar status de automação → persistir dados após redeploy ``` ``` --- ## Checklist de Revisão após o Antigravity executar Use este checklist antes de considerar o MVP concluído: ```text 1. Backend passa em pytest? 2. Frontend passa em npm run build? 3. Backend local responde /health? 4. Frontend local carrega dashboard? 5. Fluxo completo local funciona? 6. .env está ignorado? 7. Nenhuma chave aparece no GitHub? 8. OPENAI_API_KEY existe apenas no backend? 9. VITE_API_URL está configurado por ambiente? 10. CORS permite o frontend correto? 11. Backend Railway responde /health? 12. Frontend Railway abre publicamente? 13. Frontend chama backend em produção? 14. SQLite está em /app/data? 15. Volume Railway está vinculado ao backend? 16. Processo criado em produção persiste após redeploy? 17. Etapas persistem após redeploy? 18. Conexões persistem após redeploy? 19. Análise IA funciona em produção? 20. Diretrizes são geradas? 21. Status de diretriz persiste? 22. Logs não mostram dados sensíveis? 23. docs/deployment.md foi atualizado? 24. docs/security.md foi atualizado? 25. docs/homologation.md foi preenchido? 26. docs/mvp-acceptance.md foi preenchido? 27. README final está completo? ``` --- ## Próximo Passo Depois do Pacote 07, o próximo estado será **Validação / Homologação**. A prioridade será testar o MVP com 2 ou 3 processos reais da operação contábil, identificar lacunas no cadastro das etapas e ajustar o formulário antes de iniciar a Fase 2. Risco principal: usar SQLite em produção exige que o banco esteja no volume persistente correto; a validação crítica é criar dados, fazer redeploy e confirmar que os dados continuam disponíveis. ::contentReference[oaicite:1]{index=1}